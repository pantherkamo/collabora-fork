apiVersion: v1
kind: Service
metadata:
  name: collabora-service
  namespace: kamo
  labels:
    app: collabora
spec:
  type: ClusterIP
  selector:
    app: collabora
  ports:
    - name: http
      protocol: TCP
      port: 9980
      targetPort: 9980
